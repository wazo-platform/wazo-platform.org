# Serves the Docusaurus build the way production nginx must:
# clean URLs resolve to the emitted <page>.html files (trailingSlash: false).
server {
  listen 8000;
  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri.html $uri/ =404;
  }
}
